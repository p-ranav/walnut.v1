string := namespace {

    starts_with := function(str, c) {
        str.length() > 1 and str[0] == c
    };

    ends_with := function(str, c) {
        str.length() > 1 and str[str.length() - 1] == c
    };

};

"Hello World".string::starts_with('H').print();